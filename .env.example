# Copy to .env.local for local development.
# Vercel: set these in Project Settings → Environment Variables,
# separately for Preview and Production.

# === Database (if you add persistence) ===
# e.g. Postgres: postgres://USER:PASSWORD@HOST:PORT/DB?schema=public
DATABASE_URL=

# === Sentry (error monitoring) ===
# Client-side DSN must be public.
NEXT_PUBLIC_SENTRY_DSN=
# Server-side DSN (optional; can reuse NEXT_PUBLIC_SENTRY_DSN).
SENTRY_DSN=

# Used only for uploading sourcemaps during build (CI/Vercel).
# Do NOT put this in NEXT_PUBLIC_*.
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=

# === Object Storage (if you add uploads) ===
# Choose one provider: S3 or Cloudflare R2 (S3-compatible)
S3_REGION=
S3_BUCKET=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_ENDPOINT=
